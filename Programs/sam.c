#include <stdio.h>
int main()
{

    
   int a , mul = 1;
    printf("enter the number you want multiplication is ");
    scanf("%d", &a );
    printf("%d  X  %d  = %d\n", a, mul, a * mul);

    mul++;
    printf("%d  X  %d  = %d\n", a, mul, a * mul);
    mul++;
    printf("%d  X  %d  = %d\n", a, mul, a * mul);
    mul++;
    printf("%d  X  %d  = %d\n", a, mul, a * mul);
    mul++;
    printf("%d  X  %d  = %d\n", a, mul, a * mul);
    mul++;
    printf("%d  X  %d  = %d\n", a, mul, a * mul);
    mul++;
    printf("%d  X  %d  = %d\n", a, mul, a * mul);
    mul++;
    printf("%d  X  %d  = %d\n", a, mul, a * mul);
    mul++;
    printf("%d X  %d  = %d\n", a, mul, a * mul);
    mul++;
   printf("%d  x  %d  =  %d\n" ,a, mul, a* mul);
mul++; 
    return 0;
}